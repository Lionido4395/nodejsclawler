extends ../layout

block content
    .container
        .row
            each item in chapters
                .col-md-4
                    p
                        a(href='/detail/#{item.id}') #{item.title}